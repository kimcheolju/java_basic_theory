// 자바 14 새로운 switch문
// 새로운 switch는 더 많은 내용을 담고 있다.
int coupon = switch (grade) {
            case 1 -> 1000;
            case 2 -> 2000;
            case 3 -> 3000;
            default -> 500;
        };